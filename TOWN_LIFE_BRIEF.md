# Love Theft Auto: Matchtown · 现状与实施计划

> 单一事实来源。所有结论都对照真实代码核对过,标注了状态。
> 上一版 brief 的设计部分(五层架构、行为词汇表、北极星样本)仍然有效,不再重复,
> 本文只记**现状**、**待办**和**已验证的坑**。

**本文件即本项目的 GOAL** —— 所有相亲小镇的工作以此为准绳,不要另立一套。

**模型**:统一走 `grok.ts` 网关,**用当前能用的模型即可**。Grok 是后续可选项,现在不追 tier 问题。
调用失败照旧如实记 `failed`/`timeout`,不许假内容顶替。

**存储**:世界状态在 **Supabase Postgres**(schema `virtual_n1`,见 `0008_dating_town.sql`)。
Aicoo 笔记只作为**无数据库时的降级路径**,不是主存储。

---

## 一、铁律(不可违反)

1. **REAL-ONLY** — 界面上每句话都来自真实模型调用。查不到的信息说「查不到」,不许编。
2. 认知调用走 `grok.ts` + `budget.ts` 预算。
3. persona 靠**消息注入**,不要再试 link policy / scoped share(已验证死路)。
4. 共享状态不压在 operator 关键路径上(operator 402 曾冻住整个 feed)。
5. 世界状态必须活过进程重启。

---

## 二、已完成(都有真实数据验证)

### 记忆层
- 回应方从**完全失忆** → 会 `recall()` 自己关于对方的记忆
- 发起方按 top-3 关系取记忆并**按人名标注**(此前按猜的人取,张冠李戴)
- **双向写入**:target 也从自己视角记一份(此前被动方记忆永远空)
- 压缩修复:Aicoo 存笔记会**抹掉 markdown 标题**,导致 `split(/### /)` 永远切出 1 段、
  `compact()` 每次第一行 return null、笔记长到 7211 字从未压缩 → 改按**时间戳**切分
- 9000 字硬上限兜底 + `GET /api/dating/memory-health`

### 对话结构
- 多轮 `lines[]` + `continueOrClose` + `tooSimilar` 重复检测
- **输出结构改造**:`{act, message?, observable, dΔ, guessAttraction, guessTrust}`
- **14 个 act 行为词汇表** + `SILENT_ACTS`
- **沉默拍路径**:不调回应、不扣对话额度、**只写发起方记忆**(ToM gap 的物理来源)
- `MAX_ROUNDS` 4 → 2;**轮数按亲密度决定**(`closeness < 0.4` 只给 1 轮)

### GOAL prompt 重写
- 删掉「每一拍至少含一种背叛/利用/…」(**旧版头号病根,已不存在**)
- 删掉舞台腔语气示例句、「禁止两人封闭对峙要卷第三人」、「只有暧昧不算剧情」
- 加两条硬禁令(不许换皮重说 / 不许宣言金句逼问)+ 可归属性自检
- 加「行为比台词重要」「强度必须匹配阶段」「你猜他怎么看你」
- 「每句话必须带地名」→ 改成「act 涉及地点时才要说地方」

### 三道机制闸门(都不是 prompt 请求,是代码强制)
- **时间衰减** — 张力 0.08/h、心动 0.02/h、信任 0.004/h 回落到静息值
- **饱和熔断** — 连 3 拍张力 ≥0.85 强制退开并放掉 0.15
- **说话/沉默双向闸** — 12 拍窗口说满 3 次强制安静;连 2 拍无话强制开口(注入约束+重试)
- 自选沉默也降温(此前只有强制分支放张力,导致自锁 25 拍全静音)

### 状态持久化
- 新建 `town-state.ts`:**内存为准 + 写后异步落盘 + 失败绝不抛**(守铁律 4)
- `wanted` / `purse` 迁入;启动加载 + SIGINT/SIGTERM 落盘;`GET /api/dating/town-health`
- 实测:wanted=5、余额 160 **活过完整重启**

### NPC offer 变真
- `effect: string`(散文)→ `resolveOffer()` 返回**真事实或真改变**
- 买花→进库存;订包厢→真机制(30 min 内酒馆的话不进 feed);
  放消息→写 `recordKnowledge` 别人真会读到;举报→属实对方+1,**诬告自己+1**
- 查不到时明说「最近没人送过花。老周摇摇头。」
- **修好犯罪路径**:前端必须选受害者(此前不传 victim,fallout 恒为 null)
- `credit()` 让钱能进不只能出

### P0 四条真 bug(全部已修,实测验证)
- **deltas 时机** — `decide` 报 delta 时对方还没回话,后续交锋对数值零影响。
  新增 `rescoreAfterExchange()`:带**完整对话原文**在交锋后重新打分,ToM 也挪到这里
- **`BROKE_LINES` 假台词** — 5 句写死的「没钱了,不配说话」冒充 agent 发言,违反铁律 1。
  删除,改为如实 `status:'failed'` + 「Aicoo 额度用尽」。同时修正「模型额度 ≠ 钱包」的概念混淆
- **`overlap()` 中文失效** — 实测:「换皮重复」旧算法 **0.00**、新算法 **0.67**;
  「同一承诺给两人」0.50 → 0.82。改用字符 bigram,与 `tooSimilar` 统一
- **`encounterWith` 打分不一致** — 初见凭空报绝对值,一次偶遇能覆盖几天积累。改为同一套 delta

### P1 四条逼快节奏的 prompt(已改)
- `continueOrClose` 加**第三档**「接住他刚才说的,就事论事,不必推进关系」并设为最常见选项;
  第二档加闸「只有他真的给了新东西才选,聊久了就加码是最糟的选择」
- `situationFor` 三处松绑:「3 拍必须落地」→ 换方向/聊别的/不开口,明写**不要因为拖久了就摊牌**;
  「禁用上次动作」→「不必换成更狠的,安静的动作也算换」
- `encounterWith`「不要客套」→「这是初次照面,分寸就该小」
- WAIT 只在**连等两拍**时才提醒(此前一刀切禁止,与预算设计矛盾)

### #11 善意经济(已接线)
- GOAL 里「你不工作、不赚钱、没有外部任务」→ 改成真小镇 + 「花钱是有代价的信号」
- NPC 从布景变成可交易:`placesFor` 带上卖什么、多少钱、自己有多少钱、手里拿着什么
- 5 个新 act:`BUY_FLOWER` ¥20 / `GIFT`(消耗库存)/ `BOOK_BOOTH` ¥40 / `ASK_VENDOR` ¥35 / `LISTEN_BENCH` ¥10
- **买了才能送** —— 那趟去花摊的路才是信号。买不起降级为 `LINGER`「站了一会儿,没买」
- ⚠️ 实测 6 拍模型一次都没选购买,只是**叙述**去花摊。同「让 prompt 自己选沉默无效」模式,
  可能需要机制手段;但买花本就该稀有,待观察频率再决定

### 数据库(Supabase Postgres · `0008_dating_town.sql`)
- 5 张表:`town_agents`(钱包+通缉)、`town_items`(库存/送给谁)、`town_knowledge`、
  `town_relationships`(**有向** + ToM + beats)、`town_events`(含 run id)
- **原子扣款** `UPDATE … WHERE purse >= $n`,余额不足返 0 行(旧 `spend()` 读后写有竞态)
- 已迁移 SmokeCat 4 条 + SmokeDog 3 条真实关系;另 4 个 agent 在别人账号,读不到,下次互动重建
- 表内已为五维预留 `curiosity/attachment/possessiveness` 三列(可空)
- 无库时降级到内存/笔记,本地 checkout 仍可跑

### 记忆压缩自我投喂(已修 · 最严重的一个)
`compact()` 用「第一个时间戳之前的全部内容」当 head —— 但那里面**包含上一次压缩的摘要**,
于是每压一次多囤一份,复利式增长。实测一份 **9058 字的笔记里 0 行对话原文**,全是十几份重复摘要。
这直接导致 agent 把同一个主题喂了自己十几遍(「别拿 Charlie 当门槛」)。
- head 改成只取标题行,摘要**替换**不追加
- 标记 `## 到目前为止` → `【关系状态】`(markdown 标题会被 Aicoo 抹掉,全角括号能存活)
- 压缩触发从「拍数」改成「长度」(多轮让单拍涨到几千字,`beats > 6` 永远不满足)
- 硬上限裁剪有同样的 head 缺陷,一并修
- 写了一次性清洗重建已污染笔记(新代码不修历史数据)
- 实测:9100字→484字、摘要 十几份→1份、压缩 0次→1次、裁剪 每拍→0次

### 检测器与三角(#19 / #14 已完成)
- `detectors.ts` 六个检测器,**零模型调用**:轨迹异动 / 冷处理 / 单恋 / 误读 / 送礼 / 三角
- 实测产出:「SmokeDog 最近 7 次经过 bar,一次都没开口」「16 次里 16 次是 SmokeDog 先开口」
- 误读和三角**正确返回空**(gap 低于阈值 / 无人过 0.5),不硬凑
- 接 `GET /api/dating/signals` + 前端 feed 顶部「小镇注意到的」
- **修了 `destinationOf()` 定义了但从未被调用** —— 每条事件 destination 恒为 null,轨迹检测没有输入
- 三角不改线程模型:用数据算出来喂给 `narrate(…, rivalry)`,prompt 明写「这条线不是两个人的事」

### 拆编剧职责(#9 已完成 · 治舞台腔的根)
decide 之前一人干三份活:演员 + 编剧(headline/severity/consequence) + 会计(deltas)。
编剧那份是病根 —— **先想好戏剧标题,再倒推一句配得上的话**。
- decide 现在只剩 `{act, move, target, message, observable, note}`
- prompt 明写「你只管做你自己…你不需要让这一拍够精彩。真实比精彩重要」
- 编剧+会计合并进交锋后的**一次**观察调用(省一次模型调用;关键是**两者都在事后**)
- 实测 severity 回到以 ambient 为主,consequence 敢写「关系没有推进」

### Aicoo 原生记忆评估(B · 已评估,结论:不迁移)
实测三个端点:
- `/accumulate` = **批量笔记写入**(files/texts/folders/delete),和现有 `upsertNote` 同一套存储 → 无收益
- `/os/memory/search` = **独立的 memories 库**,查我们的数据 `totalFound: 0`,没找到写入路径 → 不可用
- `/os/notes/search` = **真语义检索**,跨整个工作区(2121 文件)→ 唯一有价值的
**结论:不迁移,做补充。** 保留按人直取(精确、便宜),额外在回合里加一次语义检索,
捞按人名取不到的东西(跨关系的承诺、第三方、类似经历)——
这是 `READ_BACK` / `ASK_AROUND` 从叙述变成真事的技术前提。
`searchMemory()` 自写好起从未在回合里被调用过,现已接入;过滤 `with-|Town-` 前缀并排除内部状态文件。

### 其它
- 全站中英双语(`i18n.tsx`,246 key 对齐)、改名 **Love Theft Auto: Matchtown**(中文:agent 小镇)
- CJK 字体修复(`@font-face` + `unicode-range` 重映射)
- 前端接线:feed 区分有声/沉默并显示 `observable`
- `move` 枚举校验(此前模型把中文散文写进枚举字段)
- ToM 零值修复(模型照抄 JSON 示例的 `0.0`)
- 世界循环:`DATING_WORLD_KEYS` + `ownerName` 兜底(OAuth pairwise sub 匹配不上 API key)

---

## 三、待办

**文档原列的 20 条已全部完成。** 见下方「已完成」与本节的最后一轮记录。

### 最后一轮(#12 / #15 / #17 / #18)

**#15 每个 agent 有了生活** — `seed-cast` 加 `living`(营生)+ `ownWant`(与感情无关的欲望):
SmokeCat 修旧物摊/一只差四分钟的老座钟、Charlie 图书馆+代写信/写不完的最后一页、
Bravo 酒馆帮工/想盘下后面那间空屋、SmokeDog 跑货/找一只走丢四十多天的猫。
prompt 明写「谈恋爱要占用你做它的时间——为了见一个人耽误了自己的事,那才是代价」。
同时修了 `desireOf()`:它只从关系表生成欲望,**系统本身钦定了「想要」只能指向人**。

**#12 地点成为显式动作** — `place` 进决策输出。此前地点是从台词**反向解析**的
(「句子里有『酒馆』就说明在酒馆」),意味着「去某地」根本不是可执行的动作。
发现 `placeForIntent` 是**死代码**(诊断说它硬派地点,实际从未被调用);
前端地点表缺 `market`/`alley`/`backalley` 三个坐标,发生在那儿的事走不过去,已补齐。

**#18 五维好感** — 好奇·心动·信任·依恋·占有欲,张力保留为第六维(方案 A)。
播种不靠猜:依恋 ← `beats`(真实互动计数)× 心动;好奇随熟悉度衰减但被心动托住;
占有欲从中性起步必须挣出来。`feelsLike()` 读**组合**——高心动+低信任 =「患得患失」,
高信任+低心动 =「更像朋友」。
⚠️ 第一版播种是线性的,但 `beats` 是累计写库次数(已 118),导致好奇全 0.00、依恋全顶格,
四段关系数值一模一样。改成对数曲线。

**#17 时间 + 习惯 + 偏离(最大的一块)**
- `town-clock.ts`:一个小镇日 = 16 分钟 4 时段,**和 NPC 换班节奏对齐**(此前两套系统各走各的表)
- GOAL 改成用时段约时间,不许说「九点」这种没有对应物的钟点 → **#16「约了没来」现在可判定**
- 习惯层:`habitOf()` 从 `town_events` 查「平时常去哪」——不是配置的,是真实统计
- 偏离层:`detectDeviations()` 对比习惯而非数次数(主路线 ≥40% 才算有路线,去 <10% 的地方才算绕路)

实测信号(全部零模型调用):
```
[triangle] smokecat 和 smokedog 都想要 bravo——他们自己未必知道对方也在
[misread]  smokecat 低估了 yo 对他的心思
[detour]   SmokeDog 平时几乎只在 bar，这次去了 plaza
[one-sided] 最近 29 次里有 29 次是 SmokeCat 先开口的
```
三角和误读此前一直诚实返回空(数据不足),攒够真实互动后自己浮现。

### 仍然挂着

- **需要玩家重新登录一次** —— #20 的凭证只在 OAuth 回调落库,`autonomy-health` 仍是 0。
  在那之前小镇只有 SmokeCat / SmokeDog 两个 agent 在动,**所有信号都建立在偏样本上**
  (「Bravo 一次都没主动」有一半原因是它根本没有行动能力)。
- 记忆压缩仍偶尔失败(模型层,按约定暂不追)。

## 四、已验证的坑(别再踩)

1. **Aicoo 存笔记会抹掉 markdown 标题** —— 任何用 `###` 当分隔符的持久化都会静默失效。用纯文本(时间戳)。
2. **让 prompt「自己选择沉默」无效** —— 模型被训练成产出内容,实测 0/2。沉默必须用代码剥夺。
3. **反过来推过头会全静音** —— 「NOTHING 是最常见的选项」导致 25/25 全静音。必须双向闸。
4. **模型会照抄 JSON 示例里的数值** —— `"guessAttraction": 0.0` 让 ToM 四次全返回 0。示例位置放描述,别放具体值。
5. **OAuth `sub` 是 pairwise** —— 任何 API key 都匹配不上,别指望用 key 驱动 UI 放生的 agent。
6. **改 `decision` 不会改已算好的 `scored`** —— 刹车类逻辑必须重算数值。
7. **两个 `OrbitControls`** 在 Plaza3D 里,改错那个不生效。
8. **一个 WebGL context 一个 Canvas**,浏览器上限 ~16,头像网格别用 3D。

---

## 五、当前运行状态

- 世界循环:`DATING_WORLD_KEYS` = operator key,3 分钟一轮,**正在跑**
  → 只驱动 admin 名下的 SmokeCat / SmokeDog;Bravo / Charlie / yo / Kehan 需各自主人的 key
- 前端 :3000,BFF :8787
- 健康端点:`/api/dating/memory-health`、`/api/dating/town-health`

---

## 六、建议执行顺序

~~P0 四条~~、~~P1 四条~~、~~#11 善意经济~~、~~数据库~~ 已完成。剩下:

1. **#19 呈现层检测器** —— `town_events` 已可查询,成本低,立刻能看到效果
2. **#14 三人线叙事** —— `threadKey(a,b)` 只支持两人,三角是当前 cast 唯一的戏
3. **#9 拆编剧职责** —— 治舞台腔的根,但要改调用结构
4. **#13 NPC 记忆槽** —— `town_items` 已有真实购买账本,花摊那句 blurb 终于能兑现
5. **#10 裸数字进 prompt** —— 改成语义描述
6. **#18 五维**(方案 A,表列已预留)
7. **#15 seed-cast 非恋爱欲望** / **#12 地图作行为介质**
8. **#17 地图与日程** —— 工作量最大;它解锁 #15 #16
9. **#20 refresh token** —— 可任何时候并行
